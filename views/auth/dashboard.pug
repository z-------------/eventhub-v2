doctype html
html
  head
    include /includes/head.pug
  body
    h1 #{appName} - #{pageTitle}
    
    h2 Events I own
    ul#events-list
    a(href="create-event") Create new event
    
    h2 Events I follow
    ul#following-list
    
    include /includes/footer.pug
    
    include /includes/end_scripts.pug
    
    script(src="/script/auth-dashboard.js")
